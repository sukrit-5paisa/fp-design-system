{
  "name": "fp-design-system",
  "version": "1.0.0",
  "description": "FP Design System - A comprehensive design system for FP applications, built with Flutter and showcased using Widgetbook",
  "main": "index.js",
  "scripts": {
    "build": "flutter build web --release",
    "build:github-pages": "flutter build web --release",
    "test": "flutter test",
    "clean": "flutter clean && rm -rf build/",
    "serve": "flutter run -d web-server --web-port 8080"
  },
  "keywords": [
    "flutter",
    "design-system",
    "widgetbook",
    "typography",
    "components",
    "github-pages"
  ],
  "author": "FP Design Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
